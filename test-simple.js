const express = require("express"); const path = require("path"); const app = express(); app.use("/.well-known", express.static(path.join(__dirname, ".well-known"), { setHeaders: (res, filePath) => { if (filePath.endsWith(".json") || filePath.endsWith("apple-app-site-association")) { res.setHeader("Content-Type", "application/json"); res.setHeader("Cache-Control", "no-cache"); } } })); app.listen(5001, () => console.log("Server avviato sulla porta 5001"));
